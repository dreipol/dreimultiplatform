pluginManagement {
    resolutionStrategy {
        eachPlugin {
            if (requested.id.namespace == "com.android") {
                useModule("com.android.tools.build:gradle:${versions_android_gradle_plugin}")
            }
            if (requested.id.id.startsWith("org.jetbrains.kotlin")) {
                useModule("${requested.id.id}:${requested.id.id}.gradle.plugin:${versions_kotlin}")
            }
            if (requested.id.id == "kotlinx-serialization") {
                useModule("org.jetbrains.kotlin:kotlin-serialization:${requested.version}")
            }
        }
    }
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = "dreimultiplatform"